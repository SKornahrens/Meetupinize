'use strict';

// Register `phoneList` component, along with its associated controller and template
angular.
  module('meetupinizeApp').
  component('siteFooter', {
    templateUrl: 'site-footer/site-footer.template.html'
  });
